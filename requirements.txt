# Core dependencies
sounddevice==0.4.6
numpy==1.26.0
websocket-client==1.6.1
google-generativeai==0.3.2
requests==2.31.0
aiohttp==3.8.6
python-dotenv==1.0.0

# Audio processing
pydub==0.25.1

# Google Gemini (if different from generativeai)
google-ai-generativelanguage==0.3.0

# Optional (for wave file support - already in stdlib)
# wave (Python built-in)

# Development extras
black==23.9.1
flake8==6.0.0
pytest==7.4.0

# Platform-specific notes:
# On Windows, you may need to install:
# pip install pyaudio (for sounddevice backend)
# On Linux/Mac:
# brew install portaudio (for sounddevice)
